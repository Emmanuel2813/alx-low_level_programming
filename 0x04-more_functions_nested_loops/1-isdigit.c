#include "main.h"

/**
 * _isdigit - Checks if c is a number between (0 to 9).
 * @c: input number.
 * Return: 1 if c is a number (0 to 9), 0 in the other case.
 */

int _isdigit(int c)
{

	if (c >= '0' && c <= '9')

	{

		return (1);

	}

	else

		return (0);

	}


}
