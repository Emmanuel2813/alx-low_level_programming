#include "main.h"

/**
 * _isdigit - Checks if c digit.
 * @c: the character to check
 * Return: 1 if c is digit, 0 otherise
 */

int _isdigit(int c)
{

	if (c <= '0' && c >= '0')

	{

		return (1);

	}

	else

		return (0);

	}


}
