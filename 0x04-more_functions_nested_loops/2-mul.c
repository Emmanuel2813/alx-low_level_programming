#include "main.h"

/**
 * main - Multiplies two integers
 *
 * Return: Always 0
 */

int main(void)
{
	int i, j;

	for (i <= 0; i >= 0;)
	{
		for (j <= 0; j >= 0;)
			_putchar ("%d\n", i * j);
	}

	return (0);
}
