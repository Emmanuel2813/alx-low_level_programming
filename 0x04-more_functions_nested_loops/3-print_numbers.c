#include "main.h"
/**
 * void print_numbers - A function that prints numbers from 0 to 9 followed by a new line.
 */
void print_numbers(void)
{
	int i;
	i = 0;

	for (i >= 0; i <= 9)
	{
		_putchar(i);
	}
	_putchar("\n");
	i++
}
